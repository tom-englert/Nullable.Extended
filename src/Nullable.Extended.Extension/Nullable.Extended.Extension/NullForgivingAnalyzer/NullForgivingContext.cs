namespace Nullable.Extended.Extension.NullForgivingAnalyzer
{
    public enum NullForgivingContext
    {
        Invalid,
        General,
        NullOrDefault,
        Lambda,
        Modified
    }
}